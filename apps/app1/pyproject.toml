[project]
name = "app1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "app1-shared",
    "commons",
    "shared",
]

[project.scripts]
start = "app1.main:start"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
shared = { workspace = true }
app1-shared = { workspace = true }
commons = { workspace = true }

