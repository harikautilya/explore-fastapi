[project]
name = "app2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "app2-shared",
    "commons",
    "shared",
]

[project.scripts]
app2 = "app2:main"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
shared = { workspace = true }
app2-shared = { workspace = true }
commons = { workspace = true }
